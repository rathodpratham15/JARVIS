# Frontend dev configuration. Copy to .env and edit if your backend
# is not running on the default port (5050).

# Where Vite forwards /api/* and /socket.io/* during `pnpm dev`.
VITE_API_URL=http://localhost:5050
