# Cloud-safe dependencies — excludes hardware/platform-specific packages:
# face_recognition (needs dlib+cmake), pyaudio (needs PortAudio),
# sounddevice (needs libsndfile), openai-whisper (huge model download),
# pyautogui (needs display)
# torch CPU-only index (avoids pulling 2 GB CUDA wheels)
--extra-index-url https://download.pytorch.org/whl/cpu

flask>=2.3.0
flask-cors>=4.0.0
flask-socketio>=5.3.0
python-socketio>=5.8.0
python-dotenv>=1.0.0
requests>=2.31.0
rich>=13.0.0
colorama>=0.4.6
psutil>=5.9.0
schedule>=1.2.0
pillow>=10.0.0
numpy>=1.24.0
pandas>=2.0.0
openpyxl>=3.1.0
wikipedia>=1.4.0
textblob>=0.17.0
openai>=1.0.0
anthropic>=0.40.0
google-generativeai>=0.7.0
gunicorn>=21.0.0
eventlet>=0.36.0
sqlite-vec>=0.1.0
PyJWT>=2.8.0
bcrypt>=4.0.0
google-auth>=2.28.0
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
# SAML 2.0 SP (requires libxmlsec1-dev — see nixpacks.toml)
python3-saml>=1.16.0
# Microsoft OIDC (no native deps needed)
requests>=2.31.0
# ElevenLabs TTS
elevenlabs>=1.0.0
# Face recognition (InsightFace + headless OpenCV + ONNX runtime)
insightface>=0.7.3
onnxruntime>=1.16.0
opencv-python-headless>=4.8.0
# Semantic memory embeddings (CPU-only torch pulled from index above)
torch>=2.0.0
sentence-transformers>=2.7.0
